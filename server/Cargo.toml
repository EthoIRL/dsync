[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
ctrlc = "3.4.7"
toml = "0.9.5"
serde = { version = "1.0.219", features = ["derive"] }

# Internal protocol
proto = { path = "../proto"}