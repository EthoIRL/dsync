[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["suggestions", "color", "derive"] }
tracing = "0.1.41"
tracing-subscriber = "0.3.20"
thiserror = "2.0.17"
aes = "0.8.4"

# Internal protocol
proto = { path = "../proto"}